node_modules
dist
*.tsbuildinfo
*.log
.env
.env.local
.lyy/

# K8s Secret YAML — secrets must be created via `kubectl create secret`,
# never checked in (even encrypted)
deploy/**/secret*.yaml
deploy/**/secrets/
