# packages/daemon/src

Per-profile long-lived sidecar. Boot order (from `main.ts`): acquire PID lock → load identity → create state store + pane registry → spawn MCP IPC server → connect relay client → wire router + state-sync + presence → install watchdog + signal handlers.

## Files

| File                    | What                                                                                                                                  | When to read                                                                                                         |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `bin.ts`                | Process entry — just calls `run()` from `main.ts`                                                                                     | Rarely; touch `main.ts` instead                                                                                       |
| `index.ts`              | Public barrel for `@lyy/daemon` consumers (cli / mcp)                                                                                 | Exporting something new; keeps the API surface explicit                                                              |
| `main.ts`               | `startDaemon` + `run`: PID-lock (`daemon.pid`, parent-pid aware), watchdog polling `mcp.sock` inode, 3s hard-kill on SIGTERM           | Lifecycle bugs (zombie daemons, stale PID, watchdog false positive)                                                  |
| `main.test.ts`          | `inspectPidLock` unit tests                                                                                                           | Changing PID lock behavior                                                                                           |
| `identity.ts`           | Read/write `identity.json` (peerId + JWT + relay URL)                                                                                 | Adding a field to identity; changing pair-time persistence                                                           |
| `identity.test.ts`      | Identity round-trips                                                                                                                  | Changing identity schema                                                                                             |
| `paths.ts`              | `getLyyHome`, `lyyPath` — honors `LYY_HOME` env for profile scoping                                                                   | Adding a well-known file under LYY_HOME                                                                              |
| `state.ts`              | `StateStore` — atomic tmp+rename writes to `state.json` (threads, unread counts, lastSeenSeq)                                         | Drift in unread counts, adding a state field                                                                         |
| `state.test.ts`         | State atomicity + schema                                                                                                              | Touching state schema                                                                                                |
| `state-sync.ts`         | On `connected`, parallel fetch `/peers` + `/inbox` and reconcile into state.json                                                      | Changing resync semantics                                                                                            |
| `state-sync.test.ts`    | Reconcile scenarios                                                                                                                   | Changing sync logic                                                                                                  |
| `relay-client.ts`       | socket.io-client wrapper: auto-reconnect, async disconnect (awaits engine `close` with timeout), re-emits wire events                 | Debugging reconnect loops, socket-io upgrade                                                                         |
| `relay-client.test.ts`  | Client lifecycle                                                                                                                      | Changing client behavior                                                                                             |
| `relay-http.ts`         | Typed REST client: `sendMessage`, `listInbox`, `listThreads`, `listPeers`, `archive/unarchive`, `readThread`, `markRead`, `search`    | Adding a relay REST call                                                                                             |
| `router.ts`             | `MessageRouter` — on `message:new`, upserts thread summary (uses envelope thread+peer metadata), bumps unread, emits paneInbox writes | Fixing message-routing, missing-thread cases, unread miscount                                                        |
| `router.test.ts`        | Router scenarios (self / other / pane-open / legacy envelope)                                                                         | Router behavior changes                                                                                              |
| `mcp-ipc.ts`             | Unix-socket IPC server + `McpIpcClient`: versioning, subscribers, presence + relay-status seed on subscribe, aggressive `stop()`    | Adding an IPC method, debugging TUI / MCP traffic, shutdown stalls                                                   |
| `mcp-ipc.test.ts`       | IPC round-trip + subscribers + stop-under-load                                                                                        | Changing IPC semantics                                                                                               |
| `presence.ts`            | In-memory relay presence store, attached to `RelayClient` events; emits changes for MCP fan-out                                      | Debugging online markers                                                                                             |
| `pane-registry.ts`      | Legacy `/pickup` pane tracking (per-thread pane ownership); still wired for completeness                                              | Only if restoring per-thread panes                                                                                   |
| `pane-registry.test.ts` | Pane registry                                                                                                                         | Touching pane registration                                                                                           |
| `pane-inbox.ts`         | Append-only JSONL drop for per-thread inbound messages; drained by pane hooks                                                         | Only if restoring the pane model                                                                                     |
| `pane-inbox.test.ts`    | Inbox accumulation                                                                                                                    | Changing accumulation semantics                                                                                      |
