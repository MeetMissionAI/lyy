# packages/cli/src/util

Small shared CLI helpers.

## Files

| File              | What                                                                 | When to read                                   |
| ----------------- | -------------------------------------------------------------------- | ---------------------------------------------- |
| `which.ts`        | PATH-lookup helper; returns absolute path to an executable or null   | Shelling out to an external binary             |
| `which.test.ts`   | `which()` unit tests                                                 | Changing PATH-lookup behavior                  |
